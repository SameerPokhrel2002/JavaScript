//arrow functions are like shorthand for our anonymous function. We use => operator instead of writing function in our function

let add=(num1,num2)=>num1+num2;


let result=add(1,2);
console.log(result);

let g= (user)=>`Hello ${user}!`   //=> operator is used instead of keyword function 
    

let re=g("sameer");
console.log(re);